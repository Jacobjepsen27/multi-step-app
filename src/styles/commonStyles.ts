import { css } from "../../styled-system/css";

export const headerStyle = css.raw({ fontSize: "28px", color: "marineBlue", fontWeight: "bold", mt: "32px" });
export const textStyle = css.raw({ fontSize: "14px", color: "coolGray" });